{
  "name": "pm-workflow",
  "version": "2.0.0",
  "description": "产品经理工作流参考站 — 六阶段闭环 + 数据驱动",
  "private": true,
  "scripts": {
    "prebuild": "node build-search-index.js",
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6"
  }
}
